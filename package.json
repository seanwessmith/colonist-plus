{
  "name": "colonist-ext",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build scripts/content.ts --outdir dist/scripts && cp manifest.json dist/manifest.json && cp favicon.png dist/favicon.png"
  },
  "devDependencies": {
    "bun-types": "^1.0.13"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/jsdom": "^21.1.6",
    "jsdom": "^22.1.0"
  }
}